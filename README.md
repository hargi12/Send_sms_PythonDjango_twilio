# A Python Django_twilio web application for sending SMS

A simple python-django powered web application that enables an administrator to create users and send text messages to them using Twilio API.
